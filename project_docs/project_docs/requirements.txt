# Project-wide Python dependencies
# Note: We pin SciPy below 1.11 to avoid an ArviZ compatibility issue where
# older ArviZ releases import scipy.signal.gaussian (removed in newer SciPy).
# Using this requirements.txt with `make install` will resolve the ImportError
# shown when running quantum.bayesian_optimization.

# Core runtime
numpy<2.0
scipy<1.11
matplotlib>=3.5

# Bayesian modeling stack
pymc>=4.0
pytensor>=2.10
arviz>=0.12
pandas>=1.3
xarray>=0.21
netcdf4>=1.5
